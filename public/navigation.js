(function(){'use strict';
const $=id=>document.getElementById(id);
const E=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]));
const CANONICAL=[
  {id:'efcca79e-ba17-4ed1-8a68-5df304bc9363',code:'ICC',name:'ICC Le Mans',active:true,display_order:10},
  {id:'c46acf64-716a-452f-ab31-6407a05b3656',code:'EJP',name:'Église des Jeunes Prodiges',active:true,display_order:20}
];
let portals=[...CANONICAL],portalAccess=[],activePortal=null;
const globalRole=()=>['admin','pasteur'].includes(String(currentProfile?.role||'').toLowerCase());
const portalOfCode=code=>portals.find(p=>p.code===code)||CANONICAL.find(p=>p.code===code);
const portalLabel=code=>code==='GLOBAL'?'Vision globale ICC + EJP':portalOfCode(code)?.name||code;

function injectStyle(){if($('navigationStyle'))return;const st=document.createElement('style');st.id='navigationStyle';st.textContent=`
.shell{grid-template-columns:68px minmax(0,1fr)!important;transition:grid-template-columns .2s ease}.shell.menu-open{grid-template-columns:260px minmax(0,1fr)!important}.side{padding:14px 10px!important;border-right-width:4px!important;overflow:hidden!important}.shell.menu-open .side{overflow:auto!important}.nav{display:none!important}.shell.menu-open .nav{display:grid!important}.brand{display:none!important}.shell.menu-open .brand{display:block!important;font-size:18px!important;margin:4px 6px 10px}.burger{width:44px;height:44px;border:0;border-radius:12px;background:#ffffff1f;color:#fff;font-size:25px;display:grid;place-items:center;margin:0 auto 8px}.shell.menu-open .burger{margin-left:4px}.topbar{justify-content:flex-start!important;gap:12px;align-items:center}.portal-select-wrap{display:flex;align-items:center;gap:8px;min-width:280px;margin-right:auto}.portal-select{width:100%;border:1px solid var(--line);background:#fff;border-radius:12px;padding:10px 12px;color:var(--blue);font-weight:800;box-shadow:0 4px 16px #21409a0d}.welcome-card{max-width:980px;padding:32px;margin-top:18px}.welcome-card h1{font-size:34px;line-height:1.18;margin:0 0 16px}.welcome-card p{font-size:17px;line-height:1.6}.welcome-prompt{margin-top:22px;font-weight:800;color:var(--blue)}.portal-settings-grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:12px}.portal-settings-item{padding:16px;border:1px solid var(--line);border-radius:14px;background:#fff}.portal-settings-item.locked{opacity:.68;background:#f7f8fb}.portal-access-state{display:inline-block;margin-top:7px;padding:4px 8px;border-radius:999px;font-size:11px;font-weight:800;background:#e8f7ed;color:#216e39}.portal-access-state.locked{background:#fdecec;color:#a61b1b}.menu-hidden{display:none!important}.global-health{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:12px}.global-health .card b{display:block;font-size:28px;margin-top:6px}.global-health-section{margin-top:14px}.topbar #notificationBell,.topbar .notification-bell,.topbar [data-notification-bell]{flex:0 0 auto;margin:0!important}@media(max-width:900px){.global-health{grid-template-columns:repeat(2,minmax(0,1fr))}}@media(max-width:720px){.shell,.shell.menu-open{grid-template-columns:1fr!important}.side{position:fixed!important;left:0;top:0;width:64px!important;z-index:60}.shell.menu-open .side{width:min(300px,86vw)!important;box-shadow:18px 0 45px #0004}.main{padding-left:78px!important}.topbar{align-items:stretch;flex-direction:column}.portal-select-wrap{min-width:0;margin-right:0}.welcome-card{padding:20px}.welcome-card h1{font-size:26px}.portal-settings-grid,.global-health{grid-template-columns:1fr}}
`;document.head.appendChild(st)}

function ensureShell(){injectStyle();const app=$('app'),side=document.querySelector('.side');if(!app||!side)return;if(!$('menuBurger')){const b=document.createElement('button');b.id='menuBurger';b.className='burger';b.type='button';b.setAttribute('aria-label','Ouvrir le menu');b.textContent='☰';b.onclick=()=>app.classList.toggle('menu-open');side.prepend(b)}app.classList.remove('menu-open');ensurePortalSelector();ensureWelcome();ensureBasicSettings();ensureGlobalPilotage()}
function ensurePortalSelector(){const top=document.querySelector('.topbar');if(!top)return;let wrap=$('portalSelectWrap');if(!wrap){wrap=document.createElement('div');wrap.id='portalSelectWrap';wrap.className='portal-select-wrap';wrap.innerHTML='<select id="portalSelect" class="portal-select" aria-label="Sélectionner un portail"></select>';top.prepend(wrap);$('portalSelect').addEventListener('change',onPortalChange)}renderPortalSelector()}
function hasExplicitAccess(code){const p=portalOfCode(code);return !!p&&portalAccess.some(a=>a.portal_id===p.id&&a.active!==false&&a.can_view===true)}
function hasAssignedAccess(code){const p=portalOfCode(code);return !!p&&(myAssignments||[]).some(a=>(allUnits||[]).find(u=>u.id===a.organization_unit_id)?.portal_id===p.id)}
function canUsePortalCode(code){if(globalRole())return true;if(code==='GLOBAL')return false;return hasExplicitAccess(code)||hasAssignedAccess(code)}
function renderPortalSelector(){const s=$('portalSelect');if(!s)return;const rows=[...CANONICAL,{code:'GLOBAL',name:'Vision globale ICC + EJP'}];s.innerHTML=['<option value="">Sélectionner</option>',...rows.map(p=>`<option value="${p.code}" ${canUsePortalCode(p.code)?'':'disabled'}>${E(p.name)}${canUsePortalCode(p.code)?'':' — accès non autorisé'}</option>`)].join('');s.value=activePortal||''}
async function onPortalChange(e){const next=e.target.value;if(!next){e.target.value=activePortal||'';return}if(next===activePortal)return;if(!canUsePortalCode(next)){alert('Vous ne disposez pas des droits nécessaires pour accéder à ce portail.');e.target.value=activePortal||'';return}if(activePortal){const ok=confirm(`Vous allez quitter ${portalLabel(activePortal)} pour accéder à ${portalLabel(next)}. Continuer ?`);if(!ok){e.target.value=activePortal;return}}await selectPortal(next)}

function ensureWelcome(){let p=$('portalHome');if(!p){p=document.createElement('section');p.id='portalHome';p.className='panel';p.innerHTML=`<div class="card welcome-card"><h1>Bienvenue à Impact Centre Chrétien Le Mans</h1><p>Une église où le Seigneur transforme les gens ordinaires en de véritables champions et en disciples de Jésus par la Parole de Dieu.</p><p class="welcome-prompt">Sélectionnez le portail de votre choix en haut à gauche.</p></div>`;document.querySelector('main.main')?.appendChild(p)}}
function ensureBasicSettings(){let p=$('portalSettings');if(!p){p=document.createElement('section');p.id='portalSettings';p.className='panel';document.querySelector('main.main')?.appendChild(p)}renderBasicSettings()}
function renderBasicSettings(){const p=$('portalSettings');if(!p)return;const rows=[...CANONICAL.map(x=>({code:x.code,name:x.name,ok:canUsePortalCode(x.code)})),{code:'GLOBAL',name:'Vision globale ICC + EJP',ok:canUsePortalCode('GLOBAL')}];p.innerHTML=`<div class="top"><div><h1>Paramètres généraux</h1><p>Configuration commune et accès aux portails.</p></div></div><div class="card"><h2 class="section-title">Portails disponibles</h2><div class="portal-settings-grid">${rows.map(x=>`<div class="portal-settings-item ${x.ok?'':'locked'}"><b>${E(x.name)}</b><small class="muted" style="display:block;margin-top:5px">${x.code==='GLOBAL'?'Centre de pilotage et de santé globale de l’église.':'Portail opérationnel '+E(x.code)+'.'}</small><span class="portal-access-state ${x.ok?'':'locked'}">${x.ok?'Accès autorisé':'Accès non autorisé'}</span></div>`).join('')}</div></div>`}

function ensureGlobalPilotage(){let p=$('globalPilotage');if(p)return;p=document.createElement('section');p.id='globalPilotage';p.className='panel';p.innerHTML=`<div class="top"><div><h1>Pilotage global ICC + EJP</h1><p>Santé globale de l’église, tendances consolidées, alertes et points d’attention.</p></div></div><div id="globalHealthKpis" class="global-health"><div class="card">Chargement…</div></div><div class="card global-health-section"><h2 class="section-title">Alertes & points d’attention</h2><div id="globalHealthAlerts" class="muted">Chargement…</div></div>`;document.querySelector('main.main')?.appendChild(p)}
async function loadGlobalPilotage(){ensureGlobalPilotage();const k=$('globalHealthKpis'),a=$('globalHealthAlerts');try{const [{data:aff},{count:programs},{count:issues},{count:newcomers}]=await Promise.all([db.from('member_church_affiliations').select('member_id,portal_id').eq('active',true),db.from('programs').select('*',{count:'exact',head:true}).is('archived_at',null),db.from('data_quality_issues').select('*',{count:'exact',head:true}).eq('resolved',false),db.from('members').select('*',{count:'exact',head:true}).eq('journey_status','nouveau')]);const rows=aff||[],icc=portalOfCode('ICC')?.id,ejp=portalOfCode('EJP')?.id;const all=new Set(rows.map(x=>x.member_id)).size,ci=new Set(rows.filter(x=>x.portal_id===icc).map(x=>x.member_id)).size,ce=new Set(rows.filter(x=>x.portal_id===ejp).map(x=>x.member_id)).size;k.innerHTML=`<div class="card"><span class="muted">Personnes distinctes</span><b>${all}</b></div><div class="card"><span class="muted">ICC</span><b>${ci}</b></div><div class="card"><span class="muted">EJP</span><b>${ce}</b></div><div class="card"><span class="muted">Programmes actifs</span><b>${programs??0}</b></div>`;a.innerHTML=`<div class="info-grid"><div class="info-row"><small>Qualité des données</small><b>${issues??0} anomalie(s) ouverte(s)</b></div><div class="info-row"><small>Nouveaux</small><b>${newcomers??0} personne(s)</b></div></div>`}catch(err){console.error(err);k.innerHTML='<div class="card">Impossible de charger les indicateurs.</div>';a.textContent='Impossible de charger les alertes.'}}

function isolatePanel(id){const main=document.querySelector('main.main');if(!main)return;[...main.children].forEach(el=>{if(el.tagName==='SECTION'&&el.classList.contains('panel')){const on=el.id===id;el.classList.toggle('on',on);el.style.display=on?'block':'none'}});document.querySelectorAll('[data-panel]').forEach(b=>b.classList.toggle('on',b.dataset.panel===id))}
function restoreNavLabels(buttons){const labels={dashboard:'⌂ Tableau de bord',planning:'▦ Planning',people:'◎ Personnes',ministries:'Ministères',dynamics:'Dynamiques',departments:'Départements',office:'Bureau de l\'église',settings:'⚙ Paramètres',portalSettings:'⚙ Paramètres généraux'};buttons.forEach(b=>{if(labels[b.dataset.panel])b.textContent=labels[b.dataset.panel]})}
function setNavMode(){const nav=document.querySelector('.nav');if(!nav)return;let buttons=[...nav.querySelectorAll('button[data-panel]')],seps=[...nav.querySelectorAll('.sep')];buttons.forEach(b=>b.classList.remove('menu-hidden'));seps.forEach(s=>s.classList.remove('menu-hidden'));restoreNavLabels(buttons);$('portalHomeNav')?.remove();$('globalPilotageNav')?.remove();
if(!activePortal){buttons.forEach(b=>{if(!['settings','portalSettings'].includes(b.dataset.panel))b.classList.add('menu-hidden')});seps.forEach(s=>s.classList.add('menu-hidden'));const settings=buttons.find(b=>['settings','portalSettings'].includes(b.dataset.panel));if(settings){settings.dataset.panel='portalSettings';settings.textContent='⚙ Paramètres généraux';settings.classList.remove('menu-hidden')}ensureHomeNav();return}
const basic=buttons.find(b=>b.dataset.panel==='portalSettings');if(basic){basic.dataset.panel='settings';basic.textContent='⚙ Paramètres'}
if(activePortal==='GLOBAL'){buttons.forEach(b=>{if(!['planning','settings'].includes(b.dataset.panel))b.classList.add('menu-hidden')});seps.forEach(s=>s.classList.add('menu-hidden'));const g=document.createElement('button');g.id='globalPilotageNav';g.dataset.panel='globalPilotage';g.textContent='⌂ Pilotage global';g.onclick=async()=>{await loadGlobalPilotage();isolatePanel('globalPilotage')};nav.prepend(g);const plan=buttons.find(b=>b.dataset.panel==='planning');if(plan)plan.textContent='▦ Planning global';const settings=buttons.find(b=>b.dataset.panel==='settings');if(settings)settings.textContent='⚙ Paramètres globaux';return}
const orgSep=seps.find(s=>/Organisation/i.test(s.textContent));if(orgSep)orgSep.textContent=`Organisation ${activePortal}`}
function ensureHomeNav(){const nav=document.querySelector('.nav');if(!nav||$('portalHomeNav'))return;const b=document.createElement('button');b.id='portalHomeNav';b.dataset.panel='portalHome';b.textContent='⌂ Accueil';b.onclick=()=>isolatePanel('portalHome');nav.prepend(b)}
function setBrand(){const brand=document.querySelector('.brand');if(!brand)return;if(!activePortal)brand.innerHTML='ICC LE MANS<small>Accueil général</small>';else if(activePortal==='GLOBAL')brand.innerHTML='ICC + EJP<small>Centre de pilotage global</small>';else brand.innerHTML=`${E(portalLabel(activePortal))}<small>Portail ${E(activePortal)}</small>`}

async function loadPortalData(){const [p,a]=await Promise.all([db.from('church_portals').select('*').eq('active',true).order('display_order'),currentUser?.id?db.from('profile_portal_access').select('*').eq('profile_id',currentUser.id).eq('active',true):Promise.resolve({data:[]})]);const fetched=(p.data||[]).filter(x=>['ICC','EJP'].includes(x.code));portals=CANONICAL.map(c=>({...c,...(fetched.find(x=>x.code===c.code)||{})}));portalAccess=a.data||[];window.churchPortals=portals;renderPortalSelector();renderBasicSettings()}
function renderCardsForPortal(code){const units=(allUnits||[]).filter(u=>u.active!==false);const cfg=code==='EJP'?{m:'EJP_CAT_MINISTERES',d:'EJP_CAT_DYNAMIQUES',p:'EJP_CAT_DEPARTEMENTS'}:{m:'CAT_MINISTERES',d:'CAT_DYNAMIQUES',p:'CAT_DEPARTEMENTS'};const pid=portalOfCode(code)?.id;const visible=units.filter(u=>u.scope_kind==='global'||u.portal_id===pid);const cat=c=>visible.find(u=>u.code===c)?.id;const render=(boxId,parent)=>{const box=$(boxId);if(!box)return;box.innerHTML=visible.filter(u=>u.parent_id===parent).sort((a,b)=>String(a.name).localeCompare(String(b.name),'fr',{sensitivity:'base'})).map(u=>`<div class="card unit" onclick="openEntityById('${u.id}','Accueil')"><h2>${E(u.name)}</h2><small>${E(u.description||'Espace métier')}</small></div>`).join('')||'<div class="card"><p class="muted">Aucune entité configurée.</p></div>'};render('ministryCards',cat(cfg.m));render('dynamicCards',cat(cfg.d));render('departmentCards',cat(cfg.p))}
async function selectPortal(code){if(!canUsePortalCode(code))return;activePortal=code;window.activeChurchPortal=code;sessionStorage.setItem('icc_active_portal',code);setBrand();setNavMode();renderPortalSelector();if(code==='GLOBAL'){await loadGlobalPilotage();isolatePanel('globalPilotage');return}await window.loadUnits();if(typeof window.loadCounts==='function')await window.loadCounts();if(typeof window.openPanel==='function')window.openPanel('dashboard');setTimeout(()=>isolatePanel('dashboard'),0)}
window.selectChurchPortal=selectPortal;window.getActivePortalId=()=>activePortal==='GLOBAL'||!activePortal?null:portalOfCode(activePortal)?.id||null;window.filterByActivePortal=rows=>{if(!activePortal||activePortal==='GLOBAL')return rows||[];const pid=portalOfCode(activePortal)?.id;return(rows||[]).filter(r=>r.scope_kind==='global'||r.portal_id===pid)};

function membershipField(){if($('mChurchMembership'))return;const a=$('mFirst')?.closest('.field');a?.insertAdjacentHTML('beforebegin','<div class="field full" id="mChurchMembership"><label>Appartenance à l’église</label><div class="check-grid"><label class="check-item"><input type="checkbox" id="mPortalICC"> ICC Le Mans</label><label class="check-item"><input type="checkbox" id="mPortalEJP"> Église des Jeunes Prodiges (EJP)</label></div><small class="muted">Choisissez ICC, EJP ou les deux. Une seule fiche personne est conservée.</small></div>')}
window.loadMemberPortalMembership=async id=>{membershipField();if(!$('mPortalICC'))return;$('mPortalICC').checked=!id;$('mPortalEJP').checked=false;if(!id)return;const{data}=await db.from('member_church_affiliations').select('portal_id').eq('member_id',id).eq('active',true),ids=new Set((data||[]).map(x=>x.portal_id));$('mPortalICC').checked=ids.has(portalOfCode('ICC')?.id);$('mPortalEJP').checked=ids.has(portalOfCode('EJP')?.id)};
window.saveMemberPortalMembership=async id=>{const wanted=[];if($('mPortalICC')?.checked)wanted.push(portalOfCode('ICC')?.id);if($('mPortalEJP')?.checked)wanted.push(portalOfCode('EJP')?.id);if(!wanted.length)throw Error('Choisissez ICC, EJP ou les deux.');const{data,error}=await db.from('member_church_affiliations').select('*').eq('member_id',id);if(error)throw error;for(const p of portals){const row=(data||[]).find(r=>r.portal_id===p.id),want=wanted.includes(p.id);let q;if(row)q=await db.from('member_church_affiliations').update({active:want,ends_at:want?null:new Date().toISOString().slice(0,10)}).eq('id',row.id);else if(want)q=await db.from('member_church_affiliations').insert({member_id:id,portal_id:p.id,active:true});if(q?.error)throw q.error}};
const oldLoadUnits=loadUnits;window.loadUnits=async function(){await oldLoadUnits();const{data,error}=await db.from('organization_units').select('id,name,code,unit_type,parent_id,active,display_order,description,portal_id,scope_kind').eq('active',true).order('display_order');if(!error&&data)allUnits=data;if(!portals.length)await loadPortalData();if(activePortal&&activePortal!=='GLOBAL')renderCardsForPortal(activePortal);setBrand()};
const oldMember=openMemberModal;window.openMemberModal=async id=>{await Promise.resolve(oldMember?.(id));if(!portals.length)await loadPortalData();await window.loadMemberPortalMembership(id)};
document.addEventListener('click',e=>{const panel=e.target.closest('[data-panel]');if(panel){const id=panel.dataset.panel;if(id==='portalSettings'){e.preventDefault();e.stopImmediatePropagation();isolatePanel('portalSettings')}document.getElementById('app')?.classList.remove('menu-open')}},true);
async function boot(){await loadPortalData();ensureShell();membershipField();activePortal=null;window.activeChurchPortal=null;sessionStorage.removeItem('icc_active_portal');setBrand();setNavMode();renderPortalSelector();isolatePanel('portalHome')}
const oldAll=loadAll;window.loadAll=async function(){await oldAll();await boot()};
})();